# Outputs

The original standalone SAS output files were not supplied. Report tables and figures have been extracted from the final PDF into `figures/`. If SAS output (`.pdf`, `.html`, `.rtf`, `.xlsx`) is later recovered, add it here.
